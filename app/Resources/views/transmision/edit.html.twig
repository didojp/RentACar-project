{% extends 'base.html.twig' %}

{% block main %}
    <h1>Transmision edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('transmision_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('transmision_delete', {'id': transmision.id}) }}">Delete</a>
        </li>

    </ul>
{% endblock %}
